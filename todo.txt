password reset:
    enter email
    if email exists, send email:
        email says "if u requested...click link, else ignore"
        link has their sign in token that takes them right away to password reset page


DONE: add logging for sign in/attempted sign in

add signalr teacher updates when schedule MustChangePassword

complete _StudentNavbar.cshtml

add email recovery for users with role == "teacher"
make password recovery through entering an email, if entered email matches up with email on record, send to that email, else fail


DONE:
meetings start only through teacher ui
add buttons to teacher: start next meeting, end meeting
for back to back lessons: start next meeting -> end meeting api call, clear db row, start meeting api call, populate db, send to users:
if student already in "waiting room", auto connect them as soon as row is populated and meeting is started, if not in waiting room, join lesson button routes to this lesson
"rejoin active lesson" button active till end + buffer time.




DISCONTINUED:
/*run timer every 5 mins on hour-1 (xx:04 xx:09 ...xx:54 xx:59),
call assignMeetingAsync with window of now - now + 2 for scheduled lesson start time
only activate button in ui when lesson exists, not 15 mins before as it is now.
still end class at starttime + duration + 15 mins.*/




DONE:get id of student we're editing
DONE:in studentgroupcomposition, find all where studentid = id
DONE:get groupid of these records; get unique group -> solo group. edit that group's name

DONE:when group marked as inactive, check if there's an assignment for that group, and set it as inactive.
DONE:in assignment creation/editing, for groups only show active ones
DONE:when creating assignment, dont give option to set it as inactive, active by default

DONE:cannot change people in group

DONE:when group set to inactive, deactivate assignment for that group
DONE:3 buttons for groups: deactive | and deactivate and copy | copy group's emails to clipboard (with semicolons in between to paste in gmail TO)
DONE:deactivate and copy with prepopulate group selection with the previous groups data so it's easy to change a small thing





for assignment:
when a new group is made, get sent to assignment creation/editing with that group as the prepopulated option
DONE: add isactive to dbo.group
DONE: when editing a group, add student button doesn't open the modal correctly
DONE: when editing group, add option to change use balance
DONE: only show groups where >1 person


